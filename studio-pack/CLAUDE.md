# Claude Adapter

Use `agents/00-startup-orchestrator/agent.md` as the entrypoint.

Suggested command:

```text
/startup <founder idea or workflow request>
```

Route through the Startup orchestrator first. Direct worker use is allowed only when the user names a specific HQ.
