{
  "type": "object",
  "properties": {
    "x_column": { "type": "string", "minLength": 1, "maxLength": 160, "description": "Name of the numeric x column" },
    "y_column": { "type": "string", "minLength": 1, "maxLength": 160, "description": "Name of the numeric y column" },
    "sigma_x_column": { "type": "string", "minLength": 1, "maxLength": 160, "description": "Column of positive x standard uncertainties" },
    "sigma_y_column": { "type": "string", "minLength": 1, "maxLength": 160, "description": "Column of positive y standard uncertainties" },
    "correlation_column": { "type": "string", "minLength": 1, "maxLength": 160, "description": "Optional column of per-point x–y error correlations r_i in [-1, 1]; mutually exclusive with correlation" },
    "correlation": { "type": "number", "minimum": -1, "maximum": 1, "default": 0, "description": "Single x–y error correlation applied to every point; mutually exclusive with correlation_column" },
    "range": {
      "type": "object",
      "properties": { "min": { "type": "number" }, "max": { "type": "number" } },
      "required": ["min", "max"],
      "additionalProperties": false,
      "description": "Inclusive x range of points to fit (max > min)"
    },
    "options": {
      "type": "object",
      "properties": {
        "max_iterations": { "type": "integer", "minimum": 1, "maximum": 1000, "default": 200 },
        "tolerance": { "type": "number", "minimum": 1e-15, "maximum": 0.001, "default": 1e-12, "description": "Relative slope change that stops the York iteration" },
        "curve_points": { "type": "integer", "minimum": 50, "maximum": 2000, "default": 200 }
      },
      "additionalProperties": false
    }
  },
  "required": ["x_column", "y_column", "sigma_x_column", "sigma_y_column"],
  "additionalProperties": false
}
